# Logs
node_modules
npm-debug.log*
.pnpm-debug.log*

# Dependencies
# (package-lock.json / composer.lock are committed for reproducible builds)

# Build output
.output
.tanstack
# dist/ é versionada para deploy em servidor (build de produção do front)

# Editor
.vscode/*
!.vscode/extensions.json

# OS
.DS_Store
Thumbs.db
